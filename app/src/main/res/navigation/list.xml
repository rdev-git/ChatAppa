<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/list"
    app:startDestination="@id/listFormFragment">

    <fragment
        android:id="@+id/listFormFragment"
        android:name="com.free.fast.date.flirt.meeting.app.plug.ui.list.ListFormFragment"
        android:label="Girl's profiles"
        tools:layout="@layout/fragment_list_form" >
        <action
            android:id="@+id/action_listFormFragment_to_detailFragment"
            app:destination="@id/detailFragment" />
    </fragment>
    <fragment
        android:id="@+id/detailFragment"
        android:name="com.free.fast.date.flirt.meeting.app.plug.ui.detail.DetailFragment"
        android:label="Detail Screen"
        tools:layout="@layout/fragment_detail" />
</navigation>