package com.free.fast.date.flirt.meeting.app.plug.ui.soon

import android.os.Bundle
import androidx.fragment.app.Fragment
import android.view.View
import by.kirich1409.viewbindingdelegate.viewBinding
import com.free.fast.date.flirt.meeting.app.R
import com.free.fast.date.flirt.meeting.app.databinding.FragmentSoonBinding

class SoonFragment : Fragment(R.layout.fragment_soon) {
    private val binding by viewBinding(FragmentSoonBinding::bind)

    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)
    }
}